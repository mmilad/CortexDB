{
  "name": "CortexDB (Python)",
  "install": "python3 -m pip install --upgrade pip setuptools wheel && python3 -m pip install -e '.[dev]'",
  "terminals": [
    {
      "name": "CortexDB API",
      "command": "CORTEXDB_EMBED_PROVIDER=none uvicorn app.main:app --host 127.0.0.1 --port 8000",
      "description": "REST API and MCP JSON-RPC at http://127.0.0.1:8000/mcp (embedding off for fast startup)."
    },
    {
      "name": "Tests",
      "command": "CORTEXDB_EMBED_PROVIDER=none pytest -q",
      "description": "Run all 109 tests (no Ollama required). Re-run manually after changes."
    }
  ]
}
